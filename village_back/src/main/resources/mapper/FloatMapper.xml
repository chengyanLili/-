<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.village_back.mapper.FloatMapper">

    <update id="update">
        update `floatPopulation`
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="idCard != null">
                idCard = #{idCard},
            </if>
            <if test="whenCome != null">
                whenCome = #{whenCome},
            </if>
            <if test="whereCome != null">
                whereCome = #{whereCome},
            </if>
            <if test="whyCome != null">
                whyCome = #{whyCome},
            </if>
        </set>
        <where>
            id = #{id}
        </where>

    </update>
    <select  id = "deleteBatch" resultType="java.lang.Integer">
        delete  from  floatPopulation where id in
        <foreach collection="familyIds"  item="item" index ="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="selectPage" resultType="com.example.village_back.entity.FloatPopulation">
        SELECT * FROM floatPopulation
        <!--           加这个<where></where>这个条件-->
        <where>
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="idCard != null and idCard != ''">
                and idCard like concat('%',#{idCard},'%')
            </if>
        </where>
        LIMIT #{pageNum},#{pageSize}
    </select>

    <select id="selectTotal" resultType="java.lang.Long">
        SELECT count(id) from floatPopulation
        <where>
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%',#{phone},'%')
            </if>
            <if test="idCard != null and idCard != ''">
                and idCard like concat('%',#{idCard},'%')
            </if>
        </where>
    </select>
</mapper>
